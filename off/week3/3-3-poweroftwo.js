/*
Instructions from your teacher:
Week 3-3 powerOfTwo

 Using the JavaScript language, have the function powerOfTwo(num) take the num parameter being passed which will be an integer and return the string true if it's a power of two. If it's not return the string false.

Example : if the input is 16 then your program should return the string true but if the input is 22 then the output should be the string false.
*/
var userInput = prompt("Enter a String:");

var powerOfTwo = function(str) {
  var num = Number(str);      // use num variable.

  // Your code here
}

console.log(powerOfTwo(userInput));
